<KnowledgeGraph>
  <Project NAME="telegram-transcriber-bot" VERSION="0.3.0">
    <keywords>telegram, transcription, whisper, faster-whisper, reports, deep research, go api, python workers, react ui, mcp, minio, postgres, redis, asynq, polling, webhook, docker compose</keywords>
    <annotation>GRACE baseline for migrating the current Telegram transcription bot into a local-first monorepo platform with one Go control plane and multiple thin adapters.</annotation>

    <M-CONTRACTS NAME="Contracts" TYPE="UTILITY" STATUS="implemented">
      <purpose>Own shared OpenAPI, JSON schema, webhook, WebSocket, and enum contracts that every runtime surface depends on.</purpose>
      <path>packages/contracts</path>
      <depends>none</depends>
      <verification-ref>V-M-CONTRACTS</verification-ref>
      <annotations>
        <export-openapi PURPOSE="Canonical HTTP contract definitions for clients and server." />
        <export-validationSchemas PURPOSE="Canonical JSON schema surface for HTTP, WebSocket, webhook, and internal worker DTO validation." />
        <export-websocket-schema PURPOSE="Canonical event envelope for UI live updates." />
        <export-webhook-schema PURPOSE="Canonical outbound callback envelope and delivery rules." />
        <type-JobSnapshot PURPOSE="Canonical job read model including lineage, artifacts, and children." />
        <type-DeliveryStrategy PURPOSE="Per-job delivery choice: polling or webhook." />
        <type-ArtifactResolution PURPOSE="Artifact metadata plus presigned MinIO download link." />
        <type-WorkerFinalizeRequest PURPOSE="Canonical worker finalization DTO used by all Python workers." />
        <type-SourceSet PURPOSE="Ordered input set backing one transcription job." />
        <type-BatchTranscriptionContract PURPOSE="Mixed-source `/v1/transcription-jobs/batch` contract with stable source labels, root aggregate transcription job, per-source transcription children, and aggregate artifact outputs." />
        <type-AgentRunContract PURPOSE="Request-only `agent_run_create` contract that creates root `agent_run` jobs keyed by `harness_name` without provider-specific worker routes." />
      </annotations>
    </M-CONTRACTS>

    <M-API-HTTP NAME="ApiHttp" TYPE="ENTRY_POINT" STATUS="implemented">
      <purpose>Expose create, list, get, cancel, retry, artifact, event, and internal worker endpoints for the control plane.</purpose>
      <path>apps/api/internal/api</path>
      <depends>M-CONTRACTS, M-API-JOBS, M-API-STORAGE, M-API-QUEUE, M-API-EVENTS</depends>
      <verification-ref>V-M-API-HTTP</verification-ref>
      <annotations>
        <export-registerRoutes PURPOSE="Bind public and internal HTTP routes." />
        <export-createBatchTranscriptionRoute PURPOSE="Accept `POST /v1/transcription-jobs/batch`, validate the source manifest plus multipart file parts, and return the root aggregate job snapshot." />
        <export-createAgentRunRoute PURPOSE="Accept `POST /v1/agent-runs` and force the generic `agent_run_create` submission boundary." />
        <export-validateRequest PURPOSE="Apply request validation and idempotency rules." />
        <export-handleWebsocket PURPOSE="Accept WebSocket connections for UI live updates." />
        <export-buildRuntimeDependencies PURPOSE="Assemble the concrete API runtime over jobs, storage, queue, and events." />
      </annotations>
    </M-API-HTTP>

    <M-API-JOBS NAME="ApiJobs" TYPE="CORE_LOGIC" STATUS="implemented">
      <purpose>Own job creation, lineage, status transitions, child-job reuse, retry, and cancel semantics.</purpose>
      <path>apps/api/internal/jobs</path>
      <depends>M-CONTRACTS, M-API-STORAGE, M-API-QUEUE, M-API-EVENTS</depends>
      <verification-ref>V-M-API-JOBS</verification-ref>
      <annotations>
        <export-createTranscriptionJobs PURPOSE="Create one job per uploaded file or one job from a supported URL." />
        <export-createBatchTranscriptionJob PURPOSE="Create a root aggregate transcription job, per-source transcription children, and stable source-label lineage for one mixed-source batch." />
        <export-scheduleBatchAggregate PURPOSE="Schedule `job:transcription.aggregate` for the root job after source children satisfy the batch completion policy." />
        <export-createAgentRun PURPOSE="Create one root `agent_run` job with request-only source-set semantics and redacted request metadata." />
        <export-createChildJob PURPOSE="Create or reuse canonical child jobs under validated business rules." />
        <export-cancelJob PURPOSE="Apply cooperative cancellation semantics." />
        <export-retryJob PURPOSE="Create a new lineage-linked retry row." />
        <export-transitionJob PURPOSE="Enforce allowed lifecycle transitions and version increments." />
        <type-JobLineage PURPOSE="Root, parent, and retry relationships preserved across all job types." />
        <type-BatchTranscriptionJob PURPOSE="Root aggregate transcription job with child source jobs, stable ordered source labels, manifest metadata, and aggregate diagnostics." />
      </annotations>
    </M-API-JOBS>

    <M-API-STORAGE NAME="ApiStorage" TYPE="DATA_LAYER" STATUS="implemented">
      <purpose>Persist source metadata, jobs, execution claims, artifacts, events, and webhook-delivery records while generating presigned artifact links.</purpose>
      <path>apps/api/internal/storage</path>
      <depends>M-CONTRACTS</depends>
      <verification-ref>V-M-API-STORAGE</verification-ref>
      <annotations>
        <export-persistSource PURPOSE="Write source metadata and MinIO object references." />
        <export-listJobs PURPOSE="List authoritative job rows for polling, UI, and adapter reads." />
        <export-getSourceSet PURPOSE="Read the persisted source-set shell for snapshot shaping." />
        <export-listOrderedSources PURPOSE="Resolve stable ordered source metadata bound to a source set." />
        <export-listArtifactsByJob PURPOSE="Read canonical artifact metadata for one job." />
        <export-listChildJobs PURPOSE="Read canonical child jobs for lineage-aware snapshots." />
        <export-listJobEvents PURPOSE="Read append-only timeline rows for one job." />
        <export-listDueWebhookDeliveries PURPOSE="Read pending webhook deliveries whose durable retry timestamp is due." />
        <export-upsertArtifacts PURPOSE="Register worker-produced artifact metadata after object persistence succeeds." />
        <export-claimJobExecution PURPOSE="Create authoritative execution ownership rows for queued work." />
        <export-getActiveJobExecution PURPOSE="Validate active execution ownership across worker lifecycle calls." />
        <export-finishJobExecution PURPOSE="Persist terminal execution completion for authoritative ownership release." />
        <export-resolveArtifact PURPOSE="Return artifact metadata plus presigned URL." />
        <export-newMinioObjectStoreWithPresignClient PURPOSE="Preserve internal MinIO object IO while signing artifact URLs with a public browser-reachable endpoint." />
        <type-JobSubmission PURPOSE="Idempotent create-request persistence including multi-file upload normalization." />
        <type-JobExecutionRecord PURPOSE="Storage-backed execution_id ownership record shared across claim, progress, artifact, finalize, and cancel-check flows." />
        <type-WebhookDelivery PURPOSE="Durable webhook retry state across API restarts." />
        <type-SourceSetRecord PURPOSE="Persistent ordered source set reused by single-file jobs, batch transcription roots/children, and historical combined lineage." />
        <type-BatchSourceLabel PURPOSE="Stable unique source label persisted on source_set_items with `batch-transcription.v1` label version for root and child lineage." />
        <type-AgentRunSourceSet PURPOSE="Request-only source-set shell with `input_kind=agent_run` and no source items." />
      </annotations>
    </M-API-STORAGE>

    <M-API-QUEUE NAME="ApiQueue" TYPE="INTEGRATION" STATUS="implemented">
      <purpose>Bridge job creation and background execution through asynq queues and retry policies.</purpose>
      <path>apps/api/internal/queue</path>
      <depends>M-CONTRACTS, M-API-JOBS</depends>
      <verification-ref>V-M-API-QUEUE</verification-ref>
      <annotations>
        <export-enqueue PURPOSE="Publish typed asynq tasks for each job type." />
        <type-TranscriptionAggregateQueuePolicy PURPOSE="Queue policy for root batch aggregation using the `job:transcription.aggregate` task type." />
        <type-AgentRunQueuePolicy PURPOSE="Queue policy for `agent_run` tasks using the generic `job:agent_run.run` task type." />
      </annotations>
    </M-API-QUEUE>

    <M-API-EVENTS NAME="ApiEvents" TYPE="INTEGRATION" STATUS="implemented">
      <purpose>Emit timeline, WebSocket, and webhook events from authoritative job transitions.</purpose>
      <path>apps/api/internal/ws</path>
      <depends>M-CONTRACTS, M-API-STORAGE</depends>
      <verification-ref>V-M-API-EVENTS</verification-ref>
      <annotations>
        <export-emitJobEvent PURPOSE="Persist and fan out versioned job events." />
        <export-httpWebhookDispatcher PURPOSE="POST webhook envelopes, persist attempt outcomes, and replay due deliveries with bounded retry semantics." />
        <type-JobEventEnvelope PURPOSE="Versioned event model shared across timeline, WebSocket, and webhook." />
      </annotations>
    </M-API-EVENTS>

    <M-WORKER-COMMON NAME="WorkerCommon" TYPE="UTILITY" STATUS="implemented">
      <purpose>Host shared Python execution utilities and source/domain contracts reused by transcription, the Telegram adapter, and generic agent-runner workers.</purpose>
      <path>workers/common</path>
      <depends>M-CONTRACTS</depends>
      <verification-ref>V-M-WORKER-COMMON</verification-ref>
      <annotations>
        <export-apiClient PURPOSE="Call internal worker-control endpoints consistently." />
        <export-artifactWriters PURPOSE="Write worker outputs to MinIO and return metadata payloads." />
        <export-runWorkerLoop PURPOSE="Execute one shared polling or one-shot worker loop for all worker services." />
        <export-objectStore PURPOSE="Fetch source objects and upload artifact objects through the shared MinIO adapter." />
        <export-domainContracts PURPOSE="Expose canonical source, media, transcript, report, and processed-job dataclasses outside the legacy bot package." />
        <export-sourceHelpers PURPOSE="Expose shared source extraction, source-label, and Telegram media-group helpers for workers and adapter code." />
      </annotations>
    </M-WORKER-COMMON>

    <M-WORKER-TRANSCRIPTION NAME="WorkerTranscription" TYPE="CORE_LOGIC" STATUS="implemented">
      <purpose>Execute per-source transcription jobs and root batch aggregate jobs by reusing source materialization, faster-whisper, transcript rendering, source-label ordering, and aggregate artifact persistence logic.</purpose>
      <path>workers/transcription</path>
      <depends>M-WORKER-COMMON, M-CONTRACTS</depends>
      <verification-ref>V-M-WORKER-TRANSCRIPTION</verification-ref>
      <annotations>
        <export-runTranscription PURPOSE="Claim, execute, emit progress, persist artifacts, and finalize transcription jobs." />
        <export-runTranscriptionAggregate PURPOSE="Claim a root batch job, merge successful child transcript artifacts in stable source-label order, write aggregate transcript and diagnostics artifacts, and finalize the root." />
        <export-launchTranscriptionWorker PURPOSE="Start the compose-ready transcription worker process." />
        <type-OrderedWorkerInput PURPOSE="Ordered transcription inputs resolved by the internal claim surface." />
        <type-BatchAggregateArtifacts PURPOSE="Aggregate `transcript_plain`, `transcript_segmented_markdown`, `transcript_docx`, `source_manifest_json`, and `batch_diagnostics_json` artifacts for batch roots." />
      </annotations>
    </M-WORKER-TRANSCRIPTION>

    <M-WORKER-AGENT-RUNNER NAME="AgentRunner" TYPE="CORE_LOGIC" STATUS="single-ai-worker-report-deep-research-orchestration-verified">
      <purpose>Generic AI-agent worker runtime that claims `agent_run` jobs, dispatches deterministic fixtures and first-iteration container-local Claude Code harnesses by `harness_name`, materializes input artifacts, persists report and deep-research operation artifacts plus `agent_result_json` and `execution_log`, enforces per-harness worker-local leases, resolves private request envelopes, and generates Anthropic-compatible Claude Code settings from env.</purpose>
      <path>workers/agent-runner</path>
      <depends>M-WORKER-COMMON, M-CONTRACTS, M-API-HTTP</depends>
      <verification-ref>V-M-WORKER-AGENT-RUNNER</verification-ref>
      <annotations>
        <export-runAgentHarness PURPOSE="Dispatch one generic `agent_run` job to the selected harness implementation." />
        <export-launchAgentRunnerWorker PURPOSE="Start the compose-ready generic agent-runner process through WorkerCommon runtime wiring." />
        <export-agentHarnessRegistry PURPOSE="Provide generic fixture and container-local Claude Code harness dispatch without Claude-specific worker services." />
        <export-reserveHarnessLease PURPOSE="Acquire a worker-local per-harness lease before dispatch." />
        <export-agentClaudeCodeHarness PURPOSE="Generate Claude Code settings JSON from explicit env values, resolve private request access, materialize input artifacts, and execute the container-local Claude Code CLI." />
        <export-persistOperationArtifacts PURPOSE="Persist report_markdown, report_docx, and deep_research_markdown artifacts requested by public report/deep-research operation envelopes." />
        <export-persistAgentRunSummary PURPOSE="Persist normalized result summary and redacted metadata through the current control-plane model while keeping full logs and large outputs in artifacts." />
      </annotations>
    </M-WORKER-AGENT-RUNNER>

    <M-WEB-UI NAME="WebUi" TYPE="UI_COMPONENT" STATUS="implemented">
      <purpose>Provide local file/url submission, monitoring, artifact download, and late-wave admin operations.</purpose>
      <path>apps/web</path>
      <depends>M-CONTRACTS, M-API-HTTP, M-API-EVENTS</depends>
      <verification-ref>V-M-WEB-UI</verification-ref>
      <annotations>
        <export-jobsPage PURPOSE="List and filter jobs." />
        <export-jobDetailsPage PURPOSE="Show status, lineage, artifacts, and admin actions." />
      </annotations>
    </M-WEB-UI>

    <M-TELEGRAM-ADAPTER NAME="TelegramAdapter" TYPE="INTEGRATION" STATUS="implemented">
      <purpose>Act as a thin Telegram transport client over the HTTP API while preserving current user-visible bot flows.</purpose>
      <path>apps/telegram-bot</path>
      <depends>M-CONTRACTS, M-API-HTTP</depends>
      <verification-ref>V-M-TELEGRAM-ADAPTER</verification-ref>
      <annotations>
        <export-handleIncomingMedia PURPOSE="Download Telegram media temporarily and submit it to the API." />
        <export-handleCallbacks PURPOSE="Map Telegram button callbacks to report, deep-research, retry, and segmented-artifact actions." />
        <export-submitBatchBasket PURPOSE="Submit Telegram media groups, buffered sequential media, uploaded files, and URL sources as one API batch basket without owning business orchestration." />
        <export-registerCommandMenu PURPOSE="Register the Telegram command menu for start, help, batch toggle, basket view, and basket clear." />
        <export-toggleBatchMode PURPOSE="Maintain adapter-local per-chat/user batch mode state without owning API orchestration." />
      </annotations>
    </M-TELEGRAM-ADAPTER>

    <M-MCP-ADAPTER NAME="McpAdapter" TYPE="INTEGRATION" STATUS="implemented">
      <purpose>Expose the same platform capabilities to MCP consumers through a thin adapter over the HTTP API.</purpose>
      <path>apps/mcp-server</path>
      <depends>M-CONTRACTS, M-API-HTTP</depends>
      <verification-ref>V-M-MCP-ADAPTER</verification-ref>
      <annotations>
        <export-createTranscriptionJob PURPOSE="Create transcription jobs through the MCP surface." />
        <export-inspectJob PURPOSE="Read jobs, artifacts, and child-job results." />
      </annotations>
    </M-MCP-ADAPTER>

    <M-INFRA-COMPOSE NAME="InfraCompose" TYPE="INTEGRATION" STATUS="implemented">
      <purpose>Define and bootstrap the local runtime topology for API, workers, adapters, and storage dependencies.</purpose>
      <path>infra/docker-compose.yml</path>
      <depends>M-API-HTTP, M-WORKER-TRANSCRIPTION, M-WORKER-AGENT-RUNNER, M-WEB-UI, M-TELEGRAM-ADAPTER, M-MCP-ADAPTER</depends>
      <verification-ref>V-M-INFRA-COMPOSE</verification-ref>
      <annotations>
        <export-composeStack PURPOSE="Bring up the full local stack with deterministic service boundaries." />
      </annotations>
    </M-INFRA-COMPOSE>

    <CrossLink from="M-CONTRACTS" to="M-API-HTTP" relation="defines-public-contracts-for" />
    <CrossLink from="M-API-HTTP" to="M-CONTRACTS" relation="depends-on-contracts-from" />
    <CrossLink from="M-API-HTTP" to="M-API-JOBS" relation="routes-to" />
    <CrossLink from="M-API-JOBS" to="M-API-HTTP" relation="served-through" />
    <CrossLink from="M-API-JOBS" to="M-API-STORAGE" relation="persists-through" />
    <CrossLink from="M-API-STORAGE" to="M-API-JOBS" relation="stores-state-for" />
    <CrossLink from="M-API-JOBS" to="M-API-QUEUE" relation="enqueues-through" />
    <CrossLink from="M-API-QUEUE" to="M-API-JOBS" relation="accepts-work-from" />
    <CrossLink from="M-API-JOBS" to="M-API-EVENTS" relation="emits-through" />
    <CrossLink from="M-API-EVENTS" to="M-API-JOBS" relation="observes-transitions-from" />
    <CrossLink from="M-WORKER-TRANSCRIPTION" to="M-API-HTTP" relation="claims-and-finalizes-through" />
    <CrossLink from="M-API-HTTP" to="M-WORKER-TRANSCRIPTION" relation="provides-internal-control-plane-to" />
    <CrossLink from="M-WORKER-COMMON" to="M-WORKER-TRANSCRIPTION" relation="provides-shared-worker-runtime-to" />
    <CrossLink from="M-WORKER-TRANSCRIPTION" to="M-WORKER-COMMON" relation="reuses-shared-worker-runtime-from" />
    <CrossLink from="M-WORKER-AGENT-RUNNER" to="M-API-HTTP" relation="claims-and-finalizes-through" />
    <CrossLink from="M-API-HTTP" to="M-WORKER-AGENT-RUNNER" relation="provides-internal-control-plane-to" />
    <CrossLink from="M-WORKER-COMMON" to="M-WORKER-AGENT-RUNNER" relation="provides-shared-worker-runtime-to" />
    <CrossLink from="M-WORKER-AGENT-RUNNER" to="M-WORKER-COMMON" relation="reuses-shared-worker-runtime-from" />
    <CrossLink from="M-WEB-UI" to="M-API-HTTP" relation="consumes" />
    <CrossLink from="M-API-HTTP" to="M-WEB-UI" relation="serves-data-to" />
    <CrossLink from="M-WEB-UI" to="M-API-EVENTS" relation="subscribes-to" />
    <CrossLink from="M-API-EVENTS" to="M-WEB-UI" relation="streams-events-to" />
    <CrossLink from="M-TELEGRAM-ADAPTER" to="M-API-HTTP" relation="consumes" />
    <CrossLink from="M-API-HTTP" to="M-TELEGRAM-ADAPTER" relation="serves-data-to" />
    <CrossLink from="M-MCP-ADAPTER" to="M-API-HTTP" relation="consumes" />
    <CrossLink from="M-API-HTTP" to="M-MCP-ADAPTER" relation="serves-data-to" />
    <CrossLink from="M-INFRA-COMPOSE" to="M-API-HTTP" relation="runs" />
    <CrossLink from="M-API-HTTP" to="M-INFRA-COMPOSE" relation="deployed-by" />
    <CrossLink from="M-INFRA-COMPOSE" to="M-WORKER-TRANSCRIPTION" relation="runs" />
    <CrossLink from="M-WORKER-TRANSCRIPTION" to="M-INFRA-COMPOSE" relation="deployed-by" />
    <CrossLink from="M-INFRA-COMPOSE" to="M-WORKER-AGENT-RUNNER" relation="runs" />
    <CrossLink from="M-WORKER-AGENT-RUNNER" to="M-INFRA-COMPOSE" relation="deployed-by" />
  </Project>
</KnowledgeGraph>
